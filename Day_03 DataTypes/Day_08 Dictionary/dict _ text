
Dictionary {}
--------------

--> dictionary is a collection of data
--> it denoted as {}
--> it is Ordered
--> it contain Keys and Values
  syntax:   { "key": "values","key_2": "values"}

--> dictionary is allows homogeneous and Heterogeneous
--> it is a Mutable
--> key duplication is Not possible
    values may be duplicates
--> call the  values by using Key names only
-->  won't (unable) call the values Directly
    pop(key name) --> need to mention key value inside the pop
    pop item()
    keys()
    values()
    items()
    del varibale_name[key name]